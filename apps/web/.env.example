VITE_API_URL=http://localhost:4000
# Mismo OAuth Client ID que GOOGLE_CLIENT_ID del backend (apps/api/.env).
# Déjalo vacío si aún no tienes el Client ID: la app corre sin el botón de login.
VITE_GOOGLE_CLIENT_ID=
